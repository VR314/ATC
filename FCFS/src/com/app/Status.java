package com.app;

public enum Status {
    AIRSPACE, //in airspace
    MG, //at metered gates
    APPR, //approach
    LAND, //landing
    TX, //taxiway
    RMP, //ramp
    GATE, //gate
    TKOFF, //taking off
    OUT //taken off, out of control, done
}
